{
  "devDependencies": {
    "prettier": "^2.2.1",
    "prettier-plugin-sh": "^0.6.0"
  },
  "scripts": {
    "lint": "prettier --check . && shellcheck src/**/* guest/bin/* slurm-jobs/**/*.sh",
    "prettify": "prettier --write ."
  }
}
